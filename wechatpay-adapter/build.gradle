// wechatpay-adapter :: WeChat Pay implementation.
// Depends ONLY on core-entities — never on infra or Spring.

dependencies {
    implementation project(':core-entities')
}

// No Spring Boot — plain jar only
