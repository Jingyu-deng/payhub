plugins {
    id 'java'
}

dependencies {
    implementation project(':common')
}

// 👇 Add this block to disable bootJar for library modules
tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}