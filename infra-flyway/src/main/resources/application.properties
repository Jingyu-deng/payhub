# Flyway auto-config — runs migrations automatically at application startup.
# baseline-on-migrate=true is safe for existing databases that already have tables.
spring.flyway.enabled=true
spring.flyway.locations=classpath:db
spring.flyway.baseline-on-migrate=true
