# Flyway configuration — auto-discovered by Flyway at runtime.
# Env vars (FLYWAY_URL, FLYWAY_USER, FLYWAY_PASSWORD) take precedence.
flyway.url=jdbc:postgresql://localhost:5432/payhub
flyway.user=payhub
flyway.password=payhub123
flyway.locations=classpath:db
flyway.cleanDisabled=false
